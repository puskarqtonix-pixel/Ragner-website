function clock(){document.getElementById("clock").textContent=new Date().toLocaleTimeString("en-IN",{timeZone:"Asia/Kolkata",hour12:false})+" IST"}clock();setInterval(clock,1000);
(function(){const s=document.getElementById("three");if(!s||!window.THREE)return;const scene=new THREE.Scene(),cam=new THREE.PerspectiveCamera(45,innerWidth/innerHeight,.1,100),ren=new THREE.WebGLRenderer({alpha:true,antialias:true});cam.position.z=6;ren.setPixelRatio(Math.min(devicePixelRatio,2));ren.setSize(innerWidth,innerHeight);s.appendChild(ren.domElement);const g=new THREE.Group();scene.add(g);const m1=new THREE.MeshPhysicalMaterial({color:0xd6ff67,metalness:.55,roughness:.18,clearcoat:1}),m2=new THREE.MeshPhysicalMaterial({color:0x6948ff,metalness:.35,roughness:.2,clearcoat:1});const knot=new THREE.Mesh(new THREE.TorusKnotGeometry(1.1,.3,180,28,2,3),m1);g.add(knot);for(let i=0;i<3;i++){const r=new THREE.Mesh(new THREE.TorusGeometry(1.7+i*.4,.025,10,150),i==1?m2:m1);r.rotation.x=.6*i;r.rotation.y=.4*i;g.add(r)}const cone=new THREE.Mesh(new THREE.ConeGeometry(.32,1.4,4),m2);cone.position.set(1.5,.8,.2);cone.rotation.z=-.8;g.add(cone);scene.add(new THREE.AmbientLight(0xffffff,.5));const l=new THREE.DirectionalLight(0xffffff,4);l.position.set(3,4,5);scene.add(l);const p=new THREE.PointLight(0x6948ff,30,12);p.position.set(-3,-2,4);scene.add(p);let mx=0,my=0;addEventListener("mousemove",e=>{mx=e.clientX/innerWidth-.5;my=e.clientY/innerHeight-.5});function a(){requestAnimationFrame(a);g.rotation.y+=.003;g.rotation.x+=(my*.35-g.rotation.x)*.025;g.rotation.z+=(-mx*.25-g.rotation.z)*.02;g.position.x+=(mx*1.2-g.position.x)*.02;g.position.y+=(-my*.6-g.position.y)*.02;ren.render(scene,cam)}a();addEventListener("resize",()=>{cam.aspect=innerWidth/innerHeight;cam.updateProjectionMatrix();ren.setSize(innerWidth,innerHeight)})})();